import React from 'react'

const Heading = ({text}) => {
  return (
    <div className='text-[64px] font-bold text-center flex items-center justify-center'>
      {text}
    </div>
  )
}

export default Heading
